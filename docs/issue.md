# 프로젝트 테스트 중 발생한 이슈사항 모음

- data dashboard에 table 모드와 chart 모드를 제거해줘. raw response 데이터만 있어도 괜찮아.
    - excel, csv 파일로 내보내기 할때 raw response 데이터를 가공해서 내보내기 기능을 구현하는게 좋겠어.
- data dashboard에 접기/펴기 아이콘 버튼과 삭제 아이콘 버튼의 위치를 변경해줘.
- mmproj 파일을 같이 로드한 이후 text 추론 기능이 이상해졌어.
    - 자동으로 vision 추출 기능을 활성화해서 문제가 발생하는 건지, llama-server의 옵션 설정으로 인해 문제가 발생하는 건지 잘 모르겠어. 이부분은 자세한 원인 파악을 위한 조사가 필요해.
- 다운로드 취소 이후에 생성된 repo 폴더가 삭제되지 않아.
    - 모델 파일은 삭제된는 것을 확인했어.
- vision 추출 기능이 제대로 동작하지 않아. 해결안됨.
    - pdf 파일을 이미지로 변환 후 해당 이미지를 모델이 분석하게 하는 방식이 필요해.
        - pdf를 이미지로 변환하는 라이브러리를 활용하면 좋겠어.
            - 현업에서 많이 사용할 정도로 안전하고 처리 속도가 빠르면 좋겠어.
            - 전역 설정으로 설정한 이미지 해상도로 pdf를 이미지로 변환하거나 리사이징 해놓으면 추출 속도가 빨라질 수도 있을 것 같아. (조사 필요)
    - 전역 설정으로 설정한 한번에 처리할 이미지 개수만큼 request와 response를 처리하고 response들을 하나로 합치는 과정이 필요해 보여.
        - progress bar를 추가해서 이미지 처리 진행 상황을 표시하는 것이 좋겠어.
- llama-server를 시작하는 과정에서 에러가 발생한 후 다시 llama-server를 시작하려고 하면 llama-server가 이미 실행 중이라는 에러가 발생해.
    - 실제로 llama-server가 실행 중인지 확인 후 강제로 종료하던지 화면에 실행 중이라는 표시로 변경하는 에러 핸들링이 필요해.
